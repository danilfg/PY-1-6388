# define (type-cast), methods, frozen
numbers = [1, 2, 3, 4, 2, 4, 10]
numbers_set = set(numbers)

# numbers_set.difference(set([20, 10]))
# numbers_set.intersection()

numbers_f = frozenset(numbers)

print(numbers)
print(numbers_set)
